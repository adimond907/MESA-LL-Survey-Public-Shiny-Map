library(RODBC)

conn_str <- "Driver={ODBC Driver 18 for SQL Server};
             Server=161.55.120.71, 1919;
             Database=Longline;
             Trusted_Connection=yes;
             TrustServerCertificate=yes;"

conn <- odbcDriverConnect(conn_str)

area_cpue <- sqlQuery(conn,"select * from dbo.WebAreaView where species_code=20510")
station_cpue <- sqlQuery(conn,"select * from dbo.WebStationView where species_code=20510")

write.csv(station_cpue,"data/station_cpue.csv")
